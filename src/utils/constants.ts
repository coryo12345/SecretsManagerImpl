// constants.ts

// route paths
export const routes = {
  home: '/',
  secrets: '/secrets',
  account: '/account',
};

// store action names
export const actions = {
  snackbar: {
    add: 'addSnackbarMessage',
  },
};

export default {
  routes,
  actions,
};
